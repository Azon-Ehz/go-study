syntax = "proto3";

option go_package = ".;proto";

service Greeter {
    rpc SayHello(Request) returns (HelloReply);
}

message Request {
    string name = 1;
}
message HelloReply {
    string message = 1;
}